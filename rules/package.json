{
  "name": "rules",
  "description": "Firestore Rules for Firebase",
  "type": "module",
  "scripts": {
    "serve": "firebase emulators:start --only firestore",
    "deploy": "firebase deploy --only firestore:rules",
    "test": "firebase emulators:exec --only firestore 'jest --forceExit'"
  },
  "dependencies": {
    "@faker-js/faker": "^8.0.2",
    "@firebase/firestore": "^4.1.3",
    "firebase": "^10.3.1"
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^3.0.0"
  },
  "private": true
}